"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import LocalDateTime from "./LocalDateTime";
export default function InvitationCard({party,characters}:{party:{id:string;leader:string;encounters:string;start:string};characters:{id:string;label:string}[]}){const router=useRouter();const [characterId,setCharacterId]=useState(characters[0]?.id??"");const [msg,setMsg]=useState("");async function accept(){const r=await fetch(`/api/invitations/${party.id}/accept`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({characterId:characterId||null})});setMsg(r.ok?'Accepted':'Could not accept');if(r.ok)router.refresh()}return <div className="card stack"><strong>Raid invitation from {party.leader}</strong><span>{party.encounters} · <LocalDateTime iso={party.start}/></span><div className="row"><select value={characterId} onChange={e=>setCharacterId(e.target.value)} style={{padding:10,borderRadius:10,background:'#10131a',color:'white'}}><option value="">No character yet</option>{characters.map(c=><option value={c.id} key={c.id}>{c.label}</option>)}</select><button className="btn primary" onClick={accept}>Accept</button><span className="muted">{msg}</span></div></div>}
