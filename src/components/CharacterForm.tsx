"use client";
import { useState } from "react";import { useRouter } from "next/navigation";
type C={id:string;name:string;abbreviation:string;damage_type:string;role:string};
export default function CharacterForm({classes}:{classes:C[]}){const router=useRouter();const [name,setName]=useState("");const [classId,setClassId]=useState(classes[0]?.id??"");const [msg,setMsg]=useState("");async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/profile/characters',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({characterName:name,classId})});setMsg(r.ok?'Saved':'Could not save');if(r.ok){setName('');router.refresh()}}return <form onSubmit={save} className="card form stack"><label>Character name<input value={name} onChange={e=>setName(e.target.value)} required minLength={2} maxLength={32}/></label><label>Class<select value={classId} onChange={e=>setClassId(e.target.value)}>{classes.map(c=><option key={c.id} value={c.id}>{c.name} ({c.abbreviation}) — {c.damage_type} {c.role}</option>)}</select></label><div className="row"><button className="btn primary">Add character</button><span className="muted">{msg}</span></div></form>}
