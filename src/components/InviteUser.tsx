"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export default function InviteUser({partyId,users}:{partyId:string;users:{id:string;display:string}[]}){const router=useRouter();const [userId,setUserId]=useState(users[0]?.id??"");const [msg,setMsg]=useState("");async function invite(){if(!userId)return;const r=await fetch(`/api/parties/${partyId}/invite`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId})});setMsg(r.ok?'Invitation sent':'Could not invite');if(r.ok)router.refresh()}return <div className="stack"><strong>Invite a guild member</strong><div className="row"><select value={userId} onChange={e=>setUserId(e.target.value)} style={{padding:10,borderRadius:10,background:'#10131a',color:'white'}}><option value="">Select player</option>{users.map(u=><option value={u.id} key={u.id}>{u.display}</option>)}</select><button type="button" className="btn" onClick={invite} disabled={!userId}>Invite</button><span className="muted">{msg}</span></div></div>}
