"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export default function MatchInviteButton({partyId,userId}:{partyId:string;userId:string}){const router=useRouter();const [msg,setMsg]=useState('');async function invite(){const r=await fetch(`/api/parties/${partyId}/invite`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId})});setMsg(r.ok?'Invited':'Failed');if(r.ok)router.refresh()}return <div className="row"><button className="btn primary" onClick={invite}>Invite</button><span className="muted">{msg}</span></div>}
