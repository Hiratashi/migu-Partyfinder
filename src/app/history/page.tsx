import Link from "next/link";import { requireUser } from "@/lib/auth";import { query } from "@/lib/db";import LocalDateTime from "@/components/LocalDateTime";import { archiveExpiredParties } from "@/lib/party-lifecycle";
type P={id:string;title:string|null;start_time:Date;status:string;leader:string;encounters:string};
export default async function History(){await requireUser();await archiveExpiredParties();const rows=await query<P>(`SELECT p.id,p.title,p.start_time,p.status,COALESCE(u.display_name,u.username) leader,string_agg(DISTINCT e.code, ', ' ORDER BY e.code) encounters FROM parties p JOIN users u ON u.id=p.leader_id JOIN party_encounters pe ON pe.party_id=p.id JOIN encounters e ON e.id=pe.encounter_id WHERE p.status IN ('DONE','CANCELLED') GROUP BY p.id,u.display_name,u.username ORDER BY p.start_time DESC LIMIT 200`);return <main><h1>Party history</h1><p className="muted">Completed and cancelled runs are kept out of the active Partyfinder but remain available for reference.</p><div className="grid">{rows.rows.length===0&&<div className="card muted">No archived parties yet.</div>}{rows.rows.map(p=><article className="card stack" key={p.id}><div className="row between"><strong>{p.title||'Doom Aporia'}</strong><span className="pill">{p.status}</span></div><span>{p.encounters}</span><span className="muted"><LocalDateTime iso={new Date(p.start_time).toISOString()}/> · Leader: {p.leader}</span><Link className="btn" href={`/parties/${p.id}`}>View</Link></article>)}</div></main>}
